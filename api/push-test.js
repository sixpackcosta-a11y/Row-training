const webpush=require('web-push');
async function rest(url,{method='GET',key,body}={}){const r=await fetch(url,{method,headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)});if(!r.ok)throw new Error(`supabase_${r.status}`);const t=await r.text();return t?JSON.parse(t):null;}
module.exports=async function handler(req,res){
 if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});
 const supabaseUrl=process.env.SUPABASE_URL||'https://bnvduwjisqosdjqypnvq.supabase.co', apiKey=process.env.SUPABASE_ANON_KEY||process.env.SUPABASE_PUBLISHABLE_KEY||'sb_publishable_H7nlpeJccVutM2iN3-5iHQ_WjFQGAqv', serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;
 try{const token=(req.headers.authorization||'').replace(/^Bearer\s+/i,'');const vr=await fetch(`${supabaseUrl}/auth/v1/user`,{headers:{apikey:apiKey,Authorization:`Bearer ${token}`}});if(!vr.ok)return res.status(401).json({error:'invalid_token'});const user=await vr.json();const pub=process.env.VAPID_PUBLIC_KEY,priv=process.env.VAPID_PRIVATE_KEY;if(!pub||!priv)return res.status(400).json({error:'push_not_configured'});webpush.setVapidDetails('mailto:rowtraining@example.com',pub,priv);const subs=await rest(`${supabaseUrl}/rest/v1/push_subscriptions?user_id=eq.${user.id}&select=id,endpoint,p256dh,auth`,{key:serviceKey});let sent=0;for(const s of subs||[]){try{await webpush.sendNotification({endpoint:s.endpoint,keys:{p256dh:s.p256dh,auth:s.auth}},JSON.stringify({title:'Row Training',body:'Las notificaciones push están funcionando ✅',url:'/'}));sent++;}catch(e){}}return res.json({ok:true,sent});}catch(e){return res.status(500).json({error:e.message});}
};
